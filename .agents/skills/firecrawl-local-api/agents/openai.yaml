interface:
  display_name: "Firecrawl Local API"
  short_description: "Call local Firecrawl API and CLI"
  default_prompt: "Use $firecrawl-local-api to scrape, search, crawl, batch scrape, parse documents, or choose local Firecrawl API/CLI options."
