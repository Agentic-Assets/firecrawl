interface:
  display_name: "Firecrawl Local API"
  short_description: "Call local Firecrawl endpoints"
  default_prompt: "Use $firecrawl-local-api to scrape a URL or parse a local document with the local Firecrawl API."
