"""Broker-specific legacy scraper modules.

Each subfolder owns the scraper code and source notes for one brokerage or
website family. Import top-level modules such as ``cre_scrapers.cushman`` for
backward compatibility, or import nested modules directly when editing a source.
"""
