{
  "name": "cre-collector",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-source CRE listing collector running against the local self-hosted Firecrawl API. Adapted from the Prometheus collector (scripts/firecrawl-ops/prometheus/multi_source/script.ts).",
  "type": "module",
  "scripts": {
    "collect": "tsx collect.ts",
    "typecheck": "tsc --noEmit",
    "test:unit": "node --import tsx --test tests/ts/*.test.ts tests/ts/**/*.test.ts",
    "test": "npm run typecheck && npm run test:unit",
    "validate:supabase": "python3 cre_validate.py"
  },
  "dependencies": {
    "@mendable/firecrawl-js": "^4.3.5",
    "cheerio": "^1.0.0",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "@types/node": "25.9.3",
    "@types/turndown": "^5.0.6",
    "tsx": "^4.19.0",
    "typescript": "6.0.3"
  }
}
