# Forward queue after AGENTIC-1229 CRE quality repair

This is a bounded follow-up menu, not proof that the production refresh is complete.

## Production proof

- Run the full 51-source checkpoint series from a clean immutable merged SHA with the documented 75 percent CPU watchdog and source-specific concurrency limits.
- Admit freshness only after every required source reconciles its complete inventory and the final certificate passes.
- Read back Supabase listing counts, source generations, children, jobs, and queue health without using monitor, mark-missing, status activation, or baseline-update shortcuts.

## Data integrity

- Inspect the exact Colliers target's latest watched-field history. If the parent correction predates the transactional history repair, prepare a separate fingerprinted provenance-only reconciliation and obtain the required production mutation approval before applying it.
- Bound and audit intentionally additive JLL child collections so preservation does not become unbounded historical accumulation.

## Runtime evidence

- Measure observed Colliers Coveo response sizes and sidecar RSS before changing the current 96 MiB aggregate payload ceiling or increasing the dedicated browser-batch concurrency above one.
- Record throughput, CPU, retry, timeout, and provider-challenge outcomes for each tested concurrency profile in the task progress log.
